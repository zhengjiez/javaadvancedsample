import java.awt.*;
import java.applet.*;

public class AppletSkel extends Applet {

   init() // initialize variables

   start() // Any operation before Applet is visible.

   paint() // primary method where most of codes located.

   update() // When an applet needs to be updated.

   stop() // When user leaves the applet page.

   destroy() // Release any res while applet is running, e.g. close DB

}
